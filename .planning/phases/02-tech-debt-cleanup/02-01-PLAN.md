---
phase: 02-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .zshrc
  - config/environment.zsh
  - config/aliases.zsh
  - config/functions.zsh
  - config/deps.zsh
  - config/monitor.zsh
  - config/sysinfo.zsh
autonomous: false
requirements:
  - DEBT-01
  - DEBT-02
  - DEBT-03
  - DEBT-04

must_haves:
  truths:
    - "IS_MACOS and IS_LINUX are set once in .zshrc before any module is sourced"
    - "All 11 raw OSTYPE checks across config files are replaced with IS_MACOS"
    - "PATH has no duplicate entries after exec zsh"
    - "No hardcoded ~/.dotfiles/zsh references exist in config/*.zsh or .zshrc"
  artifacts:
    - path: ".zshrc"
      provides: "IS_MACOS and IS_LINUX variable declarations"
      contains: "IS_MACOS=true"
    - path: "config/environment.zsh"
      provides: "PATH deduplication via typeset -U path"
      contains: "typeset -U path"
  key_links:
    - from: ".zshrc"
      to: "config/deps.zsh"
      via: "IS_MACOS variable available before deps.zsh is sourced"
      pattern: "IS_MACOS"
    - from: ".zshrc"
      to: "config/monitor.zsh"
      via: "IS_MACOS variable used in place of OSTYPE checks"
      pattern: "\\$IS_MACOS"
---

<objective>
Centralize platform detection, deduplicate PATH, and clean up hardcoded path references.

Purpose: Reduce duplication and fragility — 11 raw $OSTYPE checks become 2 variable declarations, PATH gets dedup protection, and documentation references are clarified.
Output: Modified .zshrc, 5 config files updated, PATH dedup added.
</objective>

<execution_context>
@/Users/uge/.claude/get-shit-done/workflows/execute-plan.md
@/Users/uge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-tech-debt-cleanup/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add IS_MACOS/IS_LINUX to .zshrc and replace all OSTYPE checks</name>
  <files>.zshrc, config/aliases.zsh, config/functions.zsh, config/deps.zsh, config/monitor.zsh, config/sysinfo.zsh</files>
  <action>
1. In `.zshrc`, add platform detection variables BEFORE the sourcing loop (before any `source config/*.zsh` lines):

```zsh
# -- Platform Detection --
# Set once here, used in all sourced modules.
[[ "$OSTYPE" == darwin* ]] && IS_MACOS=true || IS_MACOS=false
[[ "$OSTYPE" == linux*  ]] && IS_LINUX=true  || IS_LINUX=false
```

Do NOT export these variables (no `export` keyword). Plain assignment only.

2. Replace all raw `$OSTYPE` checks in config files with `$IS_MACOS` or `$IS_LINUX`:
   - `config/aliases.zsh`: Replace `[[ "$OSTYPE" == darwin* ]]` with `$IS_MACOS`. WARNING: Do NOT touch the `ls` alias BSD/GNU detection — that uses a runtime capability test (`ls --color=auto`), not OSTYPE. Leave it exactly as-is per CLAUDE.md.
   - `config/functions.zsh`: Replace the two `$OSTYPE` checks with `$IS_MACOS`.
   - `config/deps.zsh`: Replace the two `$OSTYPE` checks with `$IS_MACOS`.
   - `config/monitor.zsh`: Replace the five `$OSTYPE` checks with `$IS_MACOS`.
   - `config/sysinfo.zsh`: Replace the two `$OSTYPE` checks with `$IS_MACOS`.

For each replacement, change `[[ "$OSTYPE" == darwin* ]]` to `$IS_MACOS` and `[[ "$OSTYPE" == linux* ]]` to `$IS_LINUX`. Use the `if $IS_MACOS; then` pattern (no `[[ ]]` brackets needed since IS_MACOS is a boolean command).

3. After all replacements, verify with: `grep -n 'OSTYPE' config/*.zsh .zshrc` — only the two definition lines in .zshrc should appear.
  </action>
  <verify>
    <automated>cd /Users/uge/code/shelly && grep -c 'OSTYPE' .zshrc && grep -n 'OSTYPE' config/*.zsh | grep -v '^$' | wc -l</automated>
  </verify>
  <done>grep for OSTYPE in config/*.zsh returns 0 results. grep for OSTYPE in .zshrc returns exactly 2 lines (the definition site). IS_MACOS is used in all 5 config files where OSTYPE previously appeared.</done>
</task>

<task type="auto">
  <name>Task 2: Add PATH deduplication and clean up doc references</name>
  <files>config/environment.zsh, README.md</files>
  <action>
1. In `config/environment.zsh`, add PATH deduplication at the BOTTOM of the file (after all PATH mutations):

```zsh
# -- PATH Deduplication --
# Remove duplicate entries; preserves first-occurrence order.
typeset -U path
export PATH
```

IMPORTANT: Use lowercase `path` (the zsh array form), NOT uppercase `PATH`. `typeset -U` operates on arrays.

2. In `README.md`, find the clone/install path reference to `~/.dotfiles/zsh`. Add a clarifying note that this is a conventional location used in examples and any path works. Do NOT add a SHELLY_DIR variable to any .zsh config file — the runtime already uses ZDOTDIR_CUSTOM and REPO_DIR dynamically. DEBT-03 is documentation-only.

3. Verify no hardcoded `~/.dotfiles/zsh` references exist in config code: `grep -rn '\.dotfiles/zsh' config/ .zshrc`
  </action>
  <verify>
    <automated>cd /Users/uge/code/shelly && grep -c 'typeset -U path' config/environment.zsh && grep -rn '\.dotfiles/zsh' config/ .zshrc | wc -l</automated>
  </verify>
  <done>environment.zsh contains `typeset -U path` at the bottom. grep for `.dotfiles/zsh` in config/*.zsh and .zshrc returns 0 results. README.md clarifies the install path is user-chosen.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify no regressions (DEBT-04)</name>
  <files>.zshrc, config/environment.zsh, config/aliases.zsh, config/functions.zsh, config/deps.zsh, config/monitor.zsh, config/sysinfo.zsh</files>
  <action>Human verifies all existing functionality still works after the refactoring. No code changes in this task.</action>
  <what-built>Platform detection centralized to IS_MACOS/IS_LINUX in .zshrc, all OSTYPE checks replaced, PATH dedup added, README updated.</what-built>
  <how-to-verify>
    1. Run `exec zsh` — shell should load cleanly with no errors
    2. Run `echo $IS_MACOS` — should print `true` (on macOS) or `false` (on Linux)
    3. Run `echo $PATH | tr ':' '\n' | sort | uniq -d` — should print nothing (no duplicates)
    4. Run `sysmon status` — should show tool status with no errors
    5. Run `alias | wc -l` — should show expected alias count (same as before)
    6. Run `devtmux help` — should print help text
    7. Verify prompt renders correctly (yellow face for success, red for failure)
  </how-to-verify>
  <verify>
    <automated>cd /Users/uge/code/shelly && grep -n 'OSTYPE' config/*.zsh | wc -l && grep -c 'typeset -U path' config/environment.zsh</automated>
  </verify>
  <done>exec zsh loads cleanly, sysmon status works, all aliases resolve, devtmux help prints output, no errors visible.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `grep -n 'OSTYPE' config/*.zsh` returns zero results
- `grep -n 'OSTYPE' .zshrc` returns exactly 2 lines (definition site only)
- `grep -c 'typeset -U path' config/environment.zsh` returns 1
- `grep -rn '\.dotfiles/zsh' config/ .zshrc` returns zero results
- `exec zsh` loads cleanly
- `sysmon status` works
- All aliases resolve
</verification>

<success_criteria>
- IS_MACOS and IS_LINUX are set once in .zshrc before any sourcing
- All 11 OSTYPE checks replaced with IS_MACOS across 5 config files
- PATH dedup via typeset -U path at bottom of environment.zsh
- README clarifies install path is flexible
- No regressions in shell loading, sysmon, aliases, or devtmux
</success_criteria>

<output>
After completion, create `.planning/phases/02-tech-debt-cleanup/02-01-SUMMARY.md`
</output>
