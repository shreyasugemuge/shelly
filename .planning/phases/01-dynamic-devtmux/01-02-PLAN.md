---
phase: 01-dynamic-devtmux
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - config/aliases.zsh
autonomous: false
requirements:
  - DEV-10

must_haves:
  truths:
    - "Old hardcoded devtmux alias is removed from aliases.zsh"
    - "devtmux kill tears down the dev session"
    - "exec zsh loads cleanly with no errors"
    - "devtmux command resolves to the function (not the old alias)"
  artifacts:
    - path: "config/aliases.zsh"
      provides: "Tmux section without hardcoded devtmux alias"
  key_links:
    - from: ".zshrc"
      to: "config/functions.zsh"
      via: "source — devtmux function loaded at shell startup"
      pattern: "source.*functions"
---

<objective>
Remove the hardcoded `devtmux` alias from `config/aliases.zsh` and verify the complete devtmux function works end-to-end via `exec zsh`.

Purpose: Complete the transition from static alias to dynamic function; verify nothing is broken.
Output: Clean aliases.zsh, working devtmux command, human-verified layout.
</objective>

<execution_context>
@/Users/uge/.claude/get-shit-done/workflows/execute-plan.md
@/Users/uge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-dynamic-devtmux/01-CONTEXT.md
@.planning/phases/01-dynamic-devtmux/01-01-SUMMARY.md

@config/aliases.zsh
@config/functions.zsh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove hardcoded devtmux alias</name>
  <files>config/aliases.zsh</files>
  <action>
Remove lines 47-60 from `config/aliases.zsh` — the hardcoded `devtmux` alias that references `~/code/vipms`, `~/code/vipl-email-agent`, and `~/code/erpnext`.

Keep the `# -- Tmux --` section header and the other tmux aliases (`a_tmux`, `l_tmux`, `n_tmux`) intact. The section should look like:

```
# -- Tmux --
alias a_tmux='tmux attach -t'
alias l_tmux='tmux list-sessions'
alias n_tmux='tmux new-session -s'
```

Add a comment noting where devtmux moved:
```
# devtmux moved to config/functions.zsh (v3.0)
```

Do NOT delete the alias file or move it to to_delete — just remove the specific alias lines and add the comment.
  </action>
  <verify>
    <automated>grep -c 'devtmux' /Users/uge/code/shelly/config/aliases.zsh | grep -q '^1$' && echo "OK: only comment reference remains" || echo "FAIL: unexpected devtmux references"</automated>
  </verify>
  <done>
    - Lines 47-60 (hardcoded devtmux alias) removed from aliases.zsh
    - Other tmux aliases (a_tmux, l_tmux, n_tmux) preserved
    - Comment added noting devtmux moved to functions.zsh
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete devtmux workflow</name>
  <files>config/aliases.zsh, config/functions.zsh</files>
  <action>
Human verifies the complete devtmux function works end-to-end after alias removal. This is a visual and interactive verification that cannot be automated.
  </action>
  <verify>
    <automated>zsh -c 'source /Users/uge/code/shelly/config/functions.zsh && type devtmux && devtmux help'</automated>
  </verify>
  <done>User confirms: picker works, layout correct, Claude Code launches, session management works, kill works</done>
  <what-built>Complete devtmux function replacing the hardcoded alias. The function discovers code folders, shows an interactive project picker (fzf or numbered fallback), builds a dynamic tmux layout with Claude Code + terminal per project, and supports kill/status/help subcommands.</what-built>
  <how-to-verify>
    1. Run `exec zsh` — shell should reload with no errors
    2. Run `type devtmux` — should show "devtmux is a shell function"
    3. Run `devtmux help` — should print usage with kill/status/help subcommands
    4. Run `devtmux status` — should show session state and fzf availability
    5. Run `devtmux` — should show project picker with git repos from ~/code (with branch names)
    6. Select 1-2 projects — tmux session should open with correct column layout
    7. Verify top panes have Claude Code running, bottom panes have clean terminal
    8. Verify status bar shows magenta "devtmux" label + project names
    9. Press Ctrl-b d to detach
    10. Run `devtmux` again — should prompt "reattach or start fresh?"
    11. Run `devtmux kill` — should destroy the session
    12. Run `devtmux status` — should show "No devtmux session running"
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `exec zsh` loads without errors
- `type devtmux` shows shell function (not alias)
- `grep 'alias devtmux' config/aliases.zsh` returns nothing
- `devtmux help` prints usage
- `devtmux kill` works when session exists and when it doesn't
- Full workflow: pick projects -> tmux opens -> Claude Code in top panes -> detach -> reattach -> kill
</verification>

<success_criteria>
- Hardcoded alias completely removed from aliases.zsh
- devtmux resolves as a function, not an alias
- User confirms: picker works, layout correct, Claude Code launches, session management works
</success_criteria>

<output>
After completion, create `.planning/phases/01-dynamic-devtmux/01-02-SUMMARY.md`
</output>
